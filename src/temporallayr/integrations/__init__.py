"""
TemporalLayr framework integrations.
Each integration is optional — install the relevant extra:

    pip install temporallayr[langchain]
    pip install temporallayr[llamaindex]
    pip install temporallayr[openai]     # openai>=1.0 required
    pip install temporallayr[anthropic]  # anthropic>=0.25 required
"""
