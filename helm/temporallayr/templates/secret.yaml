apiVersion: v1
kind: Secret
metadata:
  name: {{ include "temporallayr.fullname" . }}-env
  labels:
    {{- include "temporallayr.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
