# TemporalLayr Dashboard — Environment Variables
# Copy to .env.local and fill in values

# Backend API URL (defaults to /api via Vite proxy)
VITE_API_URL=http://localhost:8000

# Optional: pre-fill API key (NOT recommended for production)
# VITE_DEFAULT_API_KEY=
